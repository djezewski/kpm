<?xml version="1.0" encoding="utf-8"?>

	<!-- 
		Each app has to have manifest file. It describes its structure (components used). 
	 -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstandroidapp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />
    
    <!--
    
    Permissions are needed to make use of protected features of the device.
    Users have possibility to agree on declared permissions upon app's installation. 
    
    <uses-permission android:name="android.permission.INTERNET"/>
    
    -->
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.firstandroidapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.firstandroidapp.SecondActivity"
            android:label="@string/hello_world" >
        </activity>
    </application>

</manifest>
